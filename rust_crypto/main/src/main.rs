// use std::fs;


// fn evaluate_replay_using_executable(solution_bytes: Vec<u8>) {
    
// }

fn main() {
    let mut i = 0;
    loop {
        // let seed = "mario";
        // let fps = 0;

        // let f = "SM64 0 Star.m64";
        // let mut solution_bytes = fs::read(f).unwrap();
        // solution_bytes = solution_bytes[0x400..].to_vec(); // remove header bytes

        // let won = evaluate_replay(seed, solution_bytes, fps);

        // println!("The returned boolean is: {}", won);
        i+=1;
        println!("{}",i)
    }
    
}
